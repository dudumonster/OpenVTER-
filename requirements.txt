# Core runtime
numpy>=1.21,<2.0
opencv-python>=4.5,<5.0
PyYAML>=6.0,<7.0
Pillow>=9.0,<11.0
shapely>=1.8,<2.1
scipy>=1.7,<1.12
pandas>=1.3,<2.1
networkx>=2.6,<3.3
filterpy>=1.4,<2.0
lap>=0.4,<1.0

# Visualization / analysis
matplotlib>=3.5,<3.9
plotly>=5.0,<6.0
streamlit>=1.20,<2.0

# PyTorch stack
# Install torch / torchvision versions that match your Python version and server CUDA.
# Example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# OpenMMLab stack
# mmcv / mmdet / mmrotate must match your torch + CUDA environment.
# Install them manually after torch is ready to avoid binary mismatch issues.
